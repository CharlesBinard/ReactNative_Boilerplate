import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    gifListBlock: {
        flexWrap: "wrap",
        flexDirection: "row",
        // maxHeight: 500,
    },
    scoreViewBlock: {},
    gifCardBlock: {
        marginHorizontal: 10,
        marginBottom: 5,
        maxWidth: "50%",
    },
    gifBlock: {
        width: 180,
        height: 95,
    },
});

export default style;
